var config = require(__dirname + '/../../src/config');

module.exports = {
  name : "name",
  date : new Date(),
  type : "test strings",
  message : "test strings",
  owner: new ObjectID(TEST_CASE_ID)
};
